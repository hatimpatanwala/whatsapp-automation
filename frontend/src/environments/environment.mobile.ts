export const environment = {
  production: true,
  // Native mobile app → always the cloud API (absolute; change to your prod domain).
  apiUrl: 'https://staging-whatsappdemo.duckdns.org/api',
  whatsapp: true,   // FULL app: WhatsApp Commerce + ERP + SFA
};
